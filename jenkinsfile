pipeline {
    agent any

    triggers {
        cron('H/1 * * * *')
    }

    stages {

        stage('Check Pending') {
            steps {
                sh '''
                curl http://192.168.59.1:8000/api/check_pending
                '''
            }
        }
    }
}