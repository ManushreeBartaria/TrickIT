pipeline {
    agent any

    triggers {
        cron('H/1 * * * *')
    }

    stages {

        stage('Check Pending') {
            steps {
                sh '''
                curl http://host.minikube.internal:8000/api/check_pending
                '''
            }
        }
    }
}